/**
 * Vertical pixel offset for angled panel edges and logo stripes.
 * Shared between CardBackground and Logo so they always match.
 * Change this single value to adjust the slant everywhere.
 */
export const SLANT_PX = 50;
